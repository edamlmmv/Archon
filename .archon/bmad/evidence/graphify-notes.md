# Graphify Notes

Observed CLI:

- `graphify` is available at `/Users/edam/.local/bin/graphify`.
- Useful commands include `query`, `path`, `explain`, `watch`, `update`, `hook status`, `merge-graphs`, and `codex install`.

V1 use:

- Use `graphify query/path/explain` as advisory repo-graph evidence.
- Do not run `graphify codex install` until the AGENTS.md impact is reviewed.

Boundary:

- Graphify output is evidence, not BMAD verifier authority.
