# UI-Lab Component Run Evidence

The `ui-lab-component-loop` workflow writes one JSON evidence artifact here for
each completed or blocked queue item.

Evidence files are generated by `.archon/scripts/ui-lab/complete.ts` only after
component-scoped validation has been recorded.
